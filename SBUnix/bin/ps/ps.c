#include <stdio.h>

int main(int argc, char* argv[]) {
        int pid; 
        pid=fork();
        if(pid ==0)
         {
         } 
         else{
             }    
        if(getpid() == 1)
          ps();   
      
        while(1);
	return 0;
}
